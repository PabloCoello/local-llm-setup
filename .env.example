# LiteLLM Master Key - Change this to a secure random key
# This key is used to authenticate requests to the LiteLLM API
LITELLM_MASTER_KEY=sk-1234-change-me-to-secure-key

# Nginx Basic Auth Credentials
# These will be used to generate the .htpasswd file
NGINX_USER=admin
NGINX_PASSWORD=change-me-secure-password

# SSL/TLS Configuration
# Set to 'self-signed' for local development or provide paths to your certificates
SSL_MODE=self-signed
SSL_CERT_PATH=./config/ssl/cert.pem
SSL_KEY_PATH=./config/ssl/key.pem

# Network Configuration
# INTERNAL_NETWORK: Your local network CIDR (e.g., 192.168.1.0/24)
INTERNAL_NETWORK=192.168.0.0/16

# Ollama Configuration
OLLAMA_HOST=0.0.0.0:11434

# Domain/Hostname (optional, for external access)
# DOMAIN=your-domain.com
